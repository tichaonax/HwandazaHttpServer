import React from 'react';
import { connect } from "react-redux";

import "../../styles/styles.css";
import './ImageGallery.css';

const gallery = props => (
    <div className="hwandaza-automation">Gallery goes here</div>
);

export default connect()(gallery);